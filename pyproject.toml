[project]
name = "digi-twin-enhanced"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "basileus", email = "invicinc@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "openai>=2.53.0",
    "pypdf>=6.15.0",
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
digi-twin-enhanced = "digi_twin_enhanced:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
